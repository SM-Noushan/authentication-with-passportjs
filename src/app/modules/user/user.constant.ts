export enum USER_ROLE {
  SUPER_ADMIN = "SUPER_ADMIN",
  ADMIN = "ADMIN",
  USER = "USER",
}

export enum USER_IS_ACTIVE {
  ACTIVE = "ACTIVE",
  INACTIVE = "INACTIVE",
  BLOCKED = "BLOCKED",
}

export enum USER_AUTH_PROVIDER {
  GOOGLE = "google",
  CREDENTIALS = "credentials",
}
